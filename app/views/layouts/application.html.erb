<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= render "shared/meta_tags" %>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= render "shared/cdn" %>
    <%= stylesheet_link_tag "application", media: "all", 'data-turbo-track': "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module", defer: true %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/chart.js", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'watchlist_navbar', 'data-turbo-track': 'reload', defer: true %>
  </head>

  <body>
    <%= render "shared/navbar" %>

    <div class="container mt-4">
      <%= render "shared/alerts" %>
      <h1 aria-hidden="true" style="display: none;"><%= content_for?(:h1) ? yield(:h1) : "Welcome to Cinematch" %></h1>
      <%= yield %>
    </div>

    <!-- Popup Modal -->
    <div id="popup" class="popup" style="display:none;">
      <div class="popup-content">
        <span class="close-button" onclick="closePopup();">&times;</span>
        <div id="popup-details"></div>
      </div>
    </div>
  </body>
</html>
